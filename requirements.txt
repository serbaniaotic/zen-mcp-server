mcp>=1.0.0
google-genai>=1.19.0
openai>=1.55.2  # Minimum version for httpx 0.28.0 compatibility
pydantic>=2.0.0
python-dotenv>=1.0.0
importlib-resources>=5.0.0; python_version<"3.9"
neo4j>=5.0.0  # MemGraph / graph database driver
gqlalchemy>=1.4.0  # Memgraph OGM for GraphRAG
datasets>=2.14.0  # HuggingFace datasets for reasoning augmentation

# Database integrations
duckdb>=1.4.0                 # Analytics engine
psycopg2-binary>=2.9.0        # PostgreSQL driver
asyncpg>=0.29.0               # Async PostgreSQL for Layer 4 (EPUB, evidence, branching)

# HTTP Bridge (for voice-QC and external integrations)
fastapi>=0.115.0              # HTTP bridge API framework
uvicorn>=0.30.0               # HTTP bridge server

# Web content extraction tools
requests>=2.31.0              # WebFetch HTTP client
youtube-transcript-api>=0.6.0 # YouTube transcripts
Wikipedia-API>=0.7.1          # Wikipedia article fetching

# Document understanding and evidence collection
docling>=2.0.0                # Multi-format document parsing
docling-core>=2.0.0           # Core document processing
pytesseract>=0.3.10           # OCR capabilities
Pillow>=10.0.0                # Image processing
yt-dlp>=2023.0.0              # YouTube video download
beautifulsoup4>=4.12.0        # HTML parsing
markdownify>=0.11.0           # HTML to Markdown conversion
PyPDF2>=3.0.0                 # PDF parsing fallback
ebooklib>=0.18                # EPUB parsing for Dogear reading system

# Vector embeddings (optional - install if using local embeddings)
# sentence-transformers>=2.2.0  # Local embeddings (free, no API needed)
# transformers>=4.30.0          # Required by sentence-transformers

# Spec generation (required for Phase 4)
PyYAML>=6.0                   # YAML parsing for frontmatter

# Development dependencies (install with pip install -r requirements-dev.txt)
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# pytest-mock>=3.11.0